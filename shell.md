# Shell Commands

* `echo $0` echoes the name of the shell
* `echo $SHELL` echos the system variable containing the shell path
* `echo $$` echoes the PID of the shell process
* `ps -p $$` prints the process information of the shell using its PID